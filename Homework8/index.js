const getWeather = () => {
  const userInputElement = document.getElementById('user-input');
  console.log(userInputElement.value);
  /* Algorithm
1. Get the input element from the DOM
2. Take the user input value
3. Perfrom validation (check wether user input value is not empty)
4. Get the data from Weather API
5. Display the data.
6. Handle errors
*/
};
